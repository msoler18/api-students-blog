<h1><%= @post.title  %></h1>
<p><%= @post.content %></p>
<% if signed_in? && @product.user == current_user %>
  <%= link_to "Edit post", edit_post_path  %>
  <%= link_to "Delete post", @post, method: :delete, data:{confirm: "Are you sure to delete this post"} %>
<% end %>  
